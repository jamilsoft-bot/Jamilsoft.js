jamilsoft.js select HTML element and perform action on the selected element

<pre>
js.action(selector)
</pre>

action : is perform on the selected element (s)
selector: select the HTML element via its tag name, class name, or id

example
<pre>
js.hide('p")
// hide all p elements
</pre>

Note:
you can the format of CSS syntax to select element in jamilsoft.js. selector are used to "find" (select) HTML elements based on their tag name, classes, id, attributes, values of attributes and much more
